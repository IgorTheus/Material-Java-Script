let color = "green";  //Armazena um valor usando "let", que é uma variável de bloco

if (color === "green") { //Verifica se a variável "color" tem o valor "green"
    
    console.log("Go");  //Se a condição for verdadeira, exiba no console a mensagem

} else if (color === "yellow") {

    console.log("Attention"); //Se a condição for verdadeira, exiba no console a mensagem

} else if (color === "red") {

    console.log("Stop") //Se a condição for verdadeira, exiba no console a mensagem

}
